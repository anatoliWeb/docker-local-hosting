http:
  routers:
    dashboard:
      rule: "Host(`traefik.home.arpa`)"
      entrypoints:
        - websecure
      service: api@internal
      tls: true
      middlewares:
        - dashboard-auth

  middlewares:
    dashboard-auth:
      basicAuth:
        users:
          - "admin:$$2y$$10$$EixZaYVK1fsbw1ZfbX3OXePaWxn96p36PQm4sEPhMNPfFhpYN76u"
