http:
  routers:
    dashboard:
      rule: "Host(`traefik.home.arpa`)"
      entrypoints:
        - websecure
      service: api@internal
      tls: true
      middlewares:
        - dashboard-auth

  middlewares:
    dashboard-auth:
      basicAuth:
        usersFile: /run/secrets/traefik-users